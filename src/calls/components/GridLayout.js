import React from 'react';

export const GridLayout = ({remoteStreams, localUserJid}) => {
   const NumberOfItemsInARow = remoteStreams.length <= 3 ? 1 : 2
   // return remoteStreams.map(_user => )
   return null;
}

export default GridLayout;
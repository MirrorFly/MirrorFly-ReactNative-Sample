const initialState = {
  chatMessages: {},
  recentChat: [],
  fromUserJId: '',
  status: 'idle',
  error: null,
  recentChatStatus: 'idle',
};

const charReducer = (state = initialState) => {
  return state;
};

export default charReducer;
